name: Build BandLock Candidate 0.6.32

on:
  push:
    branches: [ main ]
    paths:
      - 'build/BandLock-0.6.32/**'
      - '.github/workflows/build-bandlock-diag-0632.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4

      - name: Setup RootHide Theos
        uses: waruhachi/theos-action@v2.4.6
        with:
          theos-src: 'roothide/theos'
          theos-branch: 'master'
          sdks-src: 'theos/sdks'
          sdks-branch: 'master'

      - name: Validate source
        shell: bash
        run: |
          set -euo pipefail
          plutil -lint build/BandLock-0.6.32/app/Resources/Info.plist
          plutil -lint build/BandLock-0.6.32/app/Entitlements.plist
          plutil -lint build/BandLock-0.6.32/daemon/Entitlements.plist
          plutil -lint build/BandLock-0.6.32/fieldbridge/BandLockFieldBridge.plist
          plutil -lint build/BandLock-0.6.32/layout/Library/LaunchDaemons/com.gokuencinar.bandlockd.plist
          python3 -m json.tool build/BandLock-0.6.32/app/Resources/countries.json >/dev/null
          test -s build/BandLock-0.6.32/app/Resources/CreditsAvatar.jpg
          ! grep -R --include='*.m' --include='*.h' -E 'Ã.|Â.|â€¦|â€”|�' build/BandLock-0.6.32/app
          grep -q 'SUBPROJECTS += app daemon launcher fieldbridge' build/BandLock-0.6.32/Makefile
          ! grep -q 'clientprobe' build/BandLock-0.6.32/Makefile
          grep -q 'BandLock_LDFLAGS += -lroothide' build/BandLock-0.6.32/app/Makefile
          grep -q 'BLBreadcrumb.c' build/BandLock-0.6.32/app/Makefile
          grep -q 'BLLocalization.m' build/BandLock-0.6.32/app/Makefile
          grep -q 'BL_APP_LOCALIZATION=1' build/BandLock-0.6.32/app/Makefile
          grep -q 'BLCountryProfile.m' build/BandLock-0.6.32/app/Makefile
          grep -q 'BLCountryBandsViewController.m' build/BandLock-0.6.32/app/Makefile
          grep -q 'BLInfoViewController.m' build/BandLock-0.6.32/app/Makefile
          grep -q 'BLLanguageViewController.m' build/BandLock-0.6.32/app/Makefile
          grep -q 'BLFrequencyGlossaryViewController.m' build/BandLock-0.6.32/app/Makefile
          grep -q 'CFBundleShortVersionString</key><string>0.6.32</string>' build/BandLock-0.6.32/app/Resources/Info.plist
          grep -q 'com.apple.private.security.no-container' build/BandLock-0.6.32/app/Entitlements.plist
          grep -q 'com.apple.private.security.container-required' build/BandLock-0.6.32/app/Entitlements.plist
          ! grep -R --include='*.m' --include='*.h' -q 'BLProbe' build/BandLock-0.6.32/app
          ! grep -q 'diagnostic selftest fired' build/BandLock-0.6.32/app/BLControlViewController.m
          ! grep -q 'runDiagnosticSelfTest' build/BandLock-0.6.32/app/BLControlViewController.m
          ! grep -q 'RATTEST' build/BandLock-0.6.32/app/BLControlViewController.m
          ! grep -q 'RESTORETEST' build/BandLock-0.6.32/app/BLControlViewController.m
          grep -q 'initWithSupported:andActiveBands:' build/BandLock-0.6.32/daemon/BLTelephonyCore.m
          ! grep -R --include='*.m' -q 'auto refresh dispatch fired' build/BandLock-0.6.32/app
          ! grep -R --include='*.m' -q 'dispatch_after(dispatch_time(DISPATCH_TIME_NOW, (int64_t)(2.0' build/BandLock-0.6.32/app
          ! grep -q 'jbroot(' build/BandLock-0.6.32/app/BLTelephonyManager.m
          grep -q 'CFFIXED_USER_HOME' build/BandLock-0.6.32/app/BLTelephonyManager.m
          grep -q '/.jbroot-' build/BandLock-0.6.32/app/BLTelephonyManager.m
          grep -q 'BLDaemonSocketRelativePath = @"/tmp/com.gokuencinar.bandlockd.sock"' build/BandLock-0.6.32/app/BLTelephonyManager.m
          grep -q 'daemonSocketPath' build/BandLock-0.6.32/app/BLTelephonyManager.m
          grep -q 'jbroot(' build/BandLock-0.6.32/daemon/main.m
          grep -q 'com.gokuencinar.bandlockd.sock' build/BandLock-0.6.32/daemon/main.m
          grep -q 'SO_NOSIGPIPE' build/BandLock-0.6.32/app/BLTelephonyManager.m
          grep -q 'SO_RCVTIMEO' build/BandLock-0.6.32/app/BLTelephonyManager.m
          grep -q 'tv_sec = 30' build/BandLock-0.6.32/app/BLTelephonyManager.m
          grep -q 'attempt <= 40' build/BandLock-0.6.32/daemon/main.m
          grep -q 'apply retry write accepted' build/BandLock-0.6.32/daemon/main.m
          grep -q 'com.gokuencinar.bandlock.fieldtest' build/BandLock-0.6.32/daemon/BLTelephonyCore.m
          grep -q 'TWEAK_NAME = BandLockFieldBridge' build/BandLock-0.6.32/fieldbridge/Makefile
          grep -q 'com.apple.mobilephone' build/BandLock-0.6.32/fieldbridge/BandLockFieldBridge.plist
          grep -q 'com.gokuencinar.bandlock.fieldtest' build/BandLock-0.6.32/fieldbridge/FieldBridge.m
          grep -q 'launchFieldTestIfNeeded:' build/BandLock-0.6.32/fieldbridge/FieldBridge.m
          grep -q 'TPPhonePad' build/BandLock-0.6.32/fieldbridge/FieldBridge.m
          ! grep -q 'sendActionsForControlEvents' build/BandLock-0.6.32/fieldbridge/FieldBridge.m
          grep -q 'signal(SIGPIPE, SIG_IGN)' build/BandLock-0.6.32/app/BLTelephonyManager.m
          grep -q 'BandLock-client.log' build/BandLock-0.6.32/app/BLBreadcrumb.c
          grep -q 'BandLock-daemon.log' build/BandLock-0.6.32/daemon/main.m
          grep -q '@"mode_code"' build/BandLock-0.6.32/daemon/main.m
          grep -q 'result\[@"mode_code"\]' build/BandLock-0.6.32/app/BLTelephonyManager.m
          grep -q '@{@"cmd": @"fieldtest"}' build/BandLock-0.6.32/app/BLTelephonyManager.m
          ! grep -q 'tel://\*3001%2312345%23\*' build/BandLock-0.6.32/app/BLTelephonyManager.m
          ! grep -q 'CoreTelephonyClient' build/BandLock-0.6.32/app/BLTelephonyManager.m
          ! grep -q 'com.apple.CommCenter.fine-grained' build/BandLock-0.6.32/app/Entitlements.plist
          grep -q 'com.apple.CommCenter.fine-grained' build/BandLock-0.6.32/daemon/Entitlements.plist
          grep -q 'TOOL_NAME = BandLockDaemon' build/BandLock-0.6.32/daemon/Makefile
          grep -q 'TOOL_NAME = BandLockLauncher' build/BandLock-0.6.32/launcher/Makefile
          grep -q 'DOMAIN="user/501"' build/BandLock-0.6.32/layout/DEBIAN/postinst
          grep -q 'launchctl bootstrap "$DOMAIN" "$PLIST"' build/BandLock-0.6.32/layout/DEBIAN/postinst
          ! grep -q 'kickstart' build/BandLock-0.6.32/layout/DEBIAN/postinst
          ! grep -q 'launchctl .*system' build/BandLock-0.6.32/layout/DEBIAN/postinst
          grep -q 'uicache -p /Applications/BandLock.app' build/BandLock-0.6.32/layout/DEBIAN/postinst
          grep -q 'SOCKET="/tmp/${LABEL}.sock"' build/BandLock-0.6.32/layout/DEBIAN/postinst
          grep -q 'OLD_SOCKET="/tmp/bandlockd.sock"' build/BandLock-0.6.32/layout/DEBIAN/postinst
          grep -q 'killall MobilePhone' build/BandLock-0.6.32/layout/DEBIAN/postinst
          grep -q 'BLSetSelectedCountry(self.country)' build/BandLock-0.6.32/app/BLCountryDetailViewController.m
          grep -q 'Manage frequencies for %@' build/BandLock-0.6.32/app/BLControlViewController.m
          grep -q 'BandLockSelectedCountryISO' build/BandLock-0.6.32/app/BLCountryProfile.m
          grep -q 'BandLockLanguage' build/BandLock-0.6.32/app/BLLocalization.m
          grep -q 'zh-Hant' build/BandLock-0.6.32/app/BLLocalization.m
          grep -q 'zh-Hans' build/BandLock-0.6.32/app/BLLocalization.m
          grep -q 'Gokuencinar · GokuEn' build/BandLock-0.6.32/app/BLInfoViewController.m
          grep -q 'raw.githubusercontent.com/Gokuencinar/GokuEnREPO/main/Packages' build/BandLock-0.6.32/app/BLInfoViewController.m
          grep -q 'More information about frequencies' build/BandLock-0.6.32/app/BLInfoViewController.m
          ! grep -q 'BandLock Global 0.6.30' build/BandLock-0.6.32/app/BLControlViewController.m
          grep -q '^Version: 0.6.32$' build/BandLock-0.6.32/control

      - name: Build candidate
        shell: bash
        run: |
          set -euo pipefail
          cd build/BandLock-0.6.32
          make clean package FINALPACKAGE=1 THEOS_PACKAGE_SCHEME=roothide
          test -f packages/com.gokuencinar.bandlock_0.6.32_iphoneos-arm64e.deb
          APP_BIN="$(find .theos -path '*/Applications/BandLock.app/BandLock' -type f | head -n 1)"
          DAEMON_BIN="$(find .theos -path '*/usr/libexec/BandLockDaemon' -type f | head -n 1)"
          test -n "$APP_BIN"
          test -n "$DAEMON_BIN"
          command -v ldid >/dev/null 2>&1
          echo '=== App libraries ==='
          otool -L "$APP_BIN"
          otool -L "$APP_BIN" | grep -q 'libroothide.dylib'
          ldid -e "$APP_BIN" > /tmp/bandlock-app-entitlements.plist
          ldid -e "$DAEMON_BIN" > /tmp/bandlock-daemon-entitlements.plist
          grep -q 'platform-application' /tmp/bandlock-app-entitlements.plist
          grep -q 'com.apple.private.security.no-sandbox' /tmp/bandlock-app-entitlements.plist
          grep -q 'com.apple.private.security.no-container' /tmp/bandlock-app-entitlements.plist
          grep -q 'com.apple.private.security.container-required' /tmp/bandlock-app-entitlements.plist
          ! grep -q 'com.apple.CommCenter.fine-grained' /tmp/bandlock-app-entitlements.plist
          grep -q 'com.apple.CommCenter.fine-grained' /tmp/bandlock-daemon-entitlements.plist
          mkdir -p ../../diagnostics
          cp packages/com.gokuencinar.bandlock_0.6.32_iphoneos-arm64e.deb ../../diagnostics/BandLock-0.6.32-candidate.deb

      - name: Publish release candidate
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add diagnostics/BandLock-0.6.32-candidate.deb
          if ! git diff --cached --quiet; then
            git commit -m "Publish BandLock 0.6.32 candidate"
            git pull --rebase origin main
            git push
          fi

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: BandLock-0.6.32-candidate
          path: build/BandLock-0.6.32/packages/*.deb
          if-no-files-found: error
