name: Build BandLock Global 0.6.2

on:
  push:
    branches: [ main ]
    paths:
      - 'build/BandLock-0.6.2/**'
      - '.github/workflows/build-bandlock-global-062.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4

      - name: Setup RootHide Theos
        uses: waruhachi/theos-action@v2.4.6
        with:
          theos-src: 'roothide/theos'
          theos-branch: 'master'
          sdks-src: 'theos/sdks'
          sdks-branch: 'master'

      - name: Validate
        shell: bash
        run: |
          set -euo pipefail
          plutil -lint build/BandLock-0.6.2/app/Resources/Info.plist
          plutil -lint build/BandLock-0.6.2/app/Entitlements.plist
          plutil -lint build/BandLock-0.6.2/helper/Entitlements.plist
          python3 -m json.tool build/BandLock-0.6.2/app/Resources/countries.json >/dev/null
          python3 - <<'PY'
          import json
          p='build/BandLock-0.6.2/app/Resources/countries.json'
          data=json.load(open(p, encoding='utf-8-sig'))
          assert len(data.get('countries', [])) >= 150
          assert any(c.get('name') == 'Spain' and 20 in c.get('bands', []) for c in data['countries'])
          PY
          grep -q 'SUBPROJECTS += app helper' build/BandLock-0.6.2/Makefile
          grep -q 'APPLICATION_NAME = BandLock' build/BandLock-0.6.2/app/Makefile
          grep -q 'TOOL_NAME = BandLockHelper' build/BandLock-0.6.2/helper/Makefile
          grep -q 'applyPendingBandsWithCompletion:' build/BandLock-0.6.2/app/BLTelephonyManager.m
          grep -q 'posix_spawn' build/BandLock-0.6.2/app/BLTelephonyManager.m
          ! grep -q 'queryCoreTelephony' build/BandLock-0.6.2/app/BLTelephonyManager.m
          ! grep -q 'CoreTelephonyClient' build/BandLock-0.6.2/app/BLTelephonyManager.m
          ! grep -q 'com.apple.CommCenter.fine-grained' build/BandLock-0.6.2/app/Entitlements.plist
          grep -q '<string>spi</string>' build/BandLock-0.6.2/helper/Entitlements.plist
          grep -q '<string>preferences-write</string>' build/BandLock-0.6.2/helper/Entitlements.plist
          grep -q 'com.apple.CoreTelephony.CommCenterHelper.allow' build/BandLock-0.6.2/helper/Entitlements.plist
          grep -q 'NSUserDefaults.standardUserDefaults' build/BandLock-0.6.2/app/BLTelephonyManager.m
          ! grep -q 'postNotificationName:BLPendingSelectionDidChangeNotification' build/BandLock-0.6.2/app/BLTelephonyManager.m
          grep -q 'sleepForTimeInterval:0.8' build/BandLock-0.6.2/helper/main.m
          grep -q 'sleepForTimeInterval:1.0' build/BandLock-0.6.2/helper/main.m
          grep -q 'queryCoreTelephony' build/BandLock-0.6.2/helper/BLTelephonyCore.m
          grep -q 'writeLTEBands:' build/BandLock-0.6.2/helper/main.m
          grep -q 'Preparar bandas compatibles' build/BandLock-0.6.2/app/BLCountryDetailViewController.m
          grep -q 'com.gokuencinar.bandlock' build/BandLock-0.6.2/control
          test ! -d build/BandLock-0.6.2/prefs
          grep -q 'uicache -a' build/BandLock-0.6.2/layout/DEBIAN/postinst
          ! grep -R -E 'BLSpainBands|supportedSpain|activeSpain|selectOrangeSpain' build/BandLock-0.6.2

      - name: Build RootHide package
        shell: bash
        run: |
          set -euo pipefail
          cd build/BandLock-0.6.2
          make clean package FINALPACKAGE=1 THEOS_PACKAGE_SCHEME=roothide
          ls -lh packages/*.deb
          test -f packages/com.gokuencinar.bandlock_0.6.2_iphoneos-arm64e.deb
          APP_BIN="$(find .theos -path '*/Applications/BandLock.app/BandLock' -type f | head -n 1)"
          HELPER_BIN="$(find .theos -path '*/Applications/BandLock.app/BandLockHelper' -type f | head -n 1)"
          test -n "$APP_BIN"
          test -n "$HELPER_BIN"
          test -x "$HELPER_BIN"
          command -v ldid >/dev/null 2>&1
          ldid -e "$APP_BIN" > /tmp/bandlock-app-entitlements.plist
          ldid -e "$HELPER_BIN" > /tmp/bandlock-helper-entitlements.plist
          ! grep -q 'com.apple.CommCenter.fine-grained' /tmp/bandlock-app-entitlements.plist
          grep -q 'com.apple.CommCenter.fine-grained' /tmp/bandlock-helper-entitlements.plist
          grep -q '<string>spi</string>' /tmp/bandlock-helper-entitlements.plist
          grep -q '<string>preferences-write</string>' /tmp/bandlock-helper-entitlements.plist
          grep -q 'com.apple.CoreTelephony.CommCenterHelper.allow' /tmp/bandlock-helper-entitlements.plist
          cp packages/com.gokuencinar.bandlock_0.6.2_iphoneos-arm64e.deb ../../debs/

      - name: Publish package
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add debs/com.gokuencinar.bandlock_0.6.2_iphoneos-arm64e.deb
          if ! git diff --cached --quiet; then
            git commit -m "Publish BandLock Global 0.6.2"
            git push
          fi

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: BandLock-0.6.2-deb
          path: build/BandLock-0.6.2/packages/*.deb
          if-no-files-found: error

  reindex:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main

      - name: Install dpkg tools
        run: sudo apt-get update && sudo apt-get install -y dpkg-dev

      - name: Rebuild APT index
        shell: bash
        run: |
          set -euo pipefail
          git pull --ff-only
          dpkg-scanpackages -m debs /dev/null > Packages
          gzip -9 -c Packages > Packages.gz

      - name: Publish APT index
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add Packages Packages.gz
          if ! git diff --cached --quiet; then
            git commit -m "Rebuild APT index for BandLock Global 0.6.2"
            git push
          fi

